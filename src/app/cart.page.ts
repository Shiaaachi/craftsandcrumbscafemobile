import { Component } from '@angular/core';
import { IonHeader,IonToolbar,IonTitle,IonContent,IonItem,IonThumbnail,IonLabel,IonButton,IonIcon,IonList,IonNote } from '@ionic/angular/standalone';
import { addIcons } from 'ionicons'; import { add,remove,trashOutline,arrowForward } from 'ionicons/icons';
import { FirebaseService,CartItem } from './firebase.service'; import { CurrencyPipe,NgFor,NgIf,AsyncPipe } from '@angular/common'; import { Router } from '@angular/router';
@Component({selector:'app-cart',standalone:true,imports:[IonHeader,IonToolbar,IonTitle,IonContent,IonItem,IonThumbnail,IonLabel,IonButton,IonIcon,IonList,IonNote,CurrencyPipe,NgFor,NgIf,AsyncPipe],template:`
<ion-header class="cc-header"><ion-toolbar><ion-title>Your Cart</ion-title></ion-toolbar></ion-header><ion-content><div class="page-pad" *ngIf="(cart|async) as items"><div class="empty" *ngIf="!items.length">Your cart is empty.<ion-button routerLink="/menu">Browse Menu</ion-button></div><ion-list lines="none" *ngIf="items.length"><ion-item class="cart-row" *ngFor="let i of items"><ion-thumbnail slot="start"><img [src]="'assets/images/'+i.img"></ion-thumbnail><ion-label><h3>{{i.name}}</h3><p>{{i.unitPrice|currency:'PHP':'symbol':'1.0-0'}} <span *ngIf="i.selectedSize">· {{i.selectedSize}}</span></p><div class="qty"><ion-button fill="clear" (click)="change(i,-1)"><ion-icon name="remove"></ion-icon></ion-button><b>{{i.qty}}</b><ion-button fill="clear" (click)="change(i,1)"><ion-icon name="add"></ion-icon></ion-button></div></ion-label><ion-note slot="end"><b>{{i.unitPrice*i.qty|currency:'PHP':'symbol':'1.0-0'}}</b><ion-button fill="clear" color="danger" (click)="remove(i)"><ion-icon name="trash-outline"></ion-icon></ion-button></ion-note></ion-item></ion-list><div class="summary" *ngIf="items.length"><div><span>Subtotal</span><b>{{subtotal(items)|currency:'PHP':'symbol':'1.0-0'}}</b></div><div><span>Delivery</span><b>₱{{deliveryFee}}</b></div><hr><div class="total"><span>Total</span><b>{{subtotal(items)+deliveryFee|currency:'PHP':'symbol':'1.0-0'}}</b></div><ion-button expand="block" (click)="checkout()">Proceed to Checkout <ion-icon slot="end" name="arrow-forward"></ion-icon></ion-button></div></div></ion-content>`})
export class CartPage { cart=this.fb.cart$; deliveryFee=60; constructor(private fb:FirebaseService,private router:Router){addIcons({add,remove,trashOutline,arrowForward}); this.fb.deliveryFee$.subscribe(v=>this.deliveryFee=v);} subtotal(items:CartItem[]){return items.reduce((n,i)=>n+i.qty*i.unitPrice,0);} async change(i:CartItem,d:number){const items=this.fb.cart$.value; const x=items.find(a=>a.id===i.id); if(x){x.qty+=d;if(x.qty<=0)items.splice(items.indexOf(x),1);await this.fb.saveCart(items);}} async remove(i:CartItem){await this.fb.saveCart(this.fb.cart$.value.filter(x=>x.id!==i.id));} checkout(){this.router.navigateByUrl('/checkout');}}
