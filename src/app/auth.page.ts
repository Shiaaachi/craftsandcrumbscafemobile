import { Component } from '@angular/core'; import { Router } from '@angular/router'; import { IonHeader,IonToolbar,IonButtons,IonBackButton,IonTitle,IonContent,IonCard,IonCardContent,IonInput,IonItem,IonButton,IonText } from '@ionic/angular/standalone'; import { FormsModule } from '@angular/forms'; import { NgIf } from '@angular/common'; import { FirebaseService } from './firebase.service';
@Component({selector:'app-auth',standalone:true,imports:[IonHeader,IonToolbar,IonButtons,IonBackButton,IonTitle,IonContent,IonCard,IonCardContent,IonInput,IonItem,IonButton,IonText,FormsModule,NgIf],template:`<ion-header class="cc-header"><ion-toolbar><ion-buttons slot="start"><ion-back-button defaultHref="/more"></ion-back-button></ion-buttons><ion-title>{{register?'Create Account':'Welcome Back'}}</ion-title></ion-toolbar></ion-header><ion-content><div class="auth-wrap"><img src="assets/images/crumblogo.png" class="auth-logo"><ion-card><ion-card-content><ion-item *ngIf="register"><ion-input label="Full name" labelPlacement="stacked" [(ngModel)]="name"></ion-input></ion-item><ion-item *ngIf="register"><ion-input type="tel" label="Phone" labelPlacement="stacked" [(ngModel)]="phone"></ion-input></ion-item><ion-item><ion-input type="email" label="Email" labelPlacement="stacked" [(ngModel)]="email"></ion-input></ion-item><ion-item><ion-input type="password" label="Password" labelPlacement="stacked" [(ngModel)]="password"></ion-input></ion-item><p class="error" *ngIf="error">{{error}}</p><ion-button expand="block" (click)="submit()">{{register?'Create Account':'Sign In'}}</ion-button><ion-button expand="block" fill="clear" (click)="register=!register;error=''">{{register?'Already have an account? Sign in':'New here? Create an account'}}</ion-button><ion-button expand="block" fill="outline" (click)="guest()">Continue as Guest</ion-button></ion-card-content></ion-card></div></ion-content>`})
export class AuthPage {register=false;name='';phone='';email='';password='';error='';constructor(private fb:FirebaseService,private router:Router){}async submit(){try{if(this.register)await this.fb.register(this.email,this.password,this.name,this.phone);else await this.fb.login(this.email,this.password);this.router.navigateByUrl('/more');}catch(e:any){this.error=e?.message?.replace('Firebase:','').replace(/\(auth\/.*?\)/,'').trim()||'Unable to sign in.';}}async guest(){await this.fb.ensureGuest();this.router.navigateByUrl('/home');}}
